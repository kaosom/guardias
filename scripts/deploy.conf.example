# Copia como deploy.conf y rellena con los datos que te den para SSH.
# No subas deploy.conf a git (añádelo a .gitignore).

DEPLOY_HOST=guardias.universidad.edu
DEPLOY_USER=deploy
DEPLOY_KEY=~/.ssh/guardias_rsa
DEPLOY_APP_DIR=/var/www/guardias
